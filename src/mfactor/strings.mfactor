!-*- mode: factor -*-
USING: kernel memory ;
IN: strings

! string handling, which builds on byte-arrays, but counted
! strings are represented by the memory address pointing to the count byte

: uncount ( countedstr -- addr len ) dup getmem8 [ 1 + ] dip ;

! TODO obsolete in favor of `length`
: strlen ( str -- n ) getmem8 ;

: str= ( str1 str2 -- ? ) dup strlen 1 + mem= ;



